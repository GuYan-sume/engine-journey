# 学习日志

规则：写完代码后立刻写，5 行以内。**「学会了什么」必须是你自己的一句话** ——
说不出来就说明今天没真懂。那一行是这份日志唯一不能被代笔的地方。
写不出来就空着，第二天补上也要写 —— 这份日志是你四年后写简历的原料。

---

## 2026-09-14 · Day 0

- 今天做了什么：装好 VS 和 Git，建了这个仓库，跑通 hello.cpp
- 学会了什么：打开了 Visual Studio，第一次跑通一个 C++ 程序；认识了 `int`，知道它是专门用来存整数的类型
- 卡在哪里：VS 安装时忘了勾「使用 C++ 的桌面开发」，重来了一次
- 明天第一步：写温度转换程序
- 用时：2 小时

---

## 2026-09-15 · Day 1

- 今天做了什么：给 hello.cpp 加了个 `int` 变量存年龄；晚上用 `cout` 一行行拼出了马里奥字符画；中文打印成乱码，查出是编码问题并修好
- 学会了什么：学会用 `cout` 一行一行往下输出，把字符画拼成马里奥（当时还没跑起来，报错说找不到 `bits/stdc++.h`）
- 卡在哪里：屏幕上出现「骞撮緞:18」，一开始以为是 `int age = 18;` 写错了，其实是控制台拿 GBK 去读 UTF-8 的字节（2 个中文字变 3 个乱码字，就是 3:2 这个比例露的馅）
- 明天第一步：修 chaojimali.cpp 的「万能头」和「两个 main」
- 用时：约 1.5 小时

---

## 2026-09-16 · Day 2

- 今天做了什么：把马里奥字符画跑起来（换掉 GCC 万能头、排掉重复的 main）；做了个双击就能运行的 `看马里奥.cmd`
- 学会了什么：搞懂了 UTF-8 和 GBK 的冲突 —— 同一串字节，换一张表去读就是不同的字；顺带弄明白编译和链接是两步，LNK2005 是链接阶段才有的报错
- 卡在哪里：网上抄来的 `#include <bits/stdc++.h>` 在 MSVC 里根本不存在（那是 GCC 专有的）；写 .cmd 脚本时用了 LF 行尾，被 cmd 吃掉了每行第一个字符
- 明天第一步：写温度转换（输入 100 出 212；输入 37 出 98.6）
- 用时：约 1 小时

---

## 2026-09-17 · Day 3

- 今天做了什么：写了温度转换程序（读入摄氏 → 换算 → 输出华氏），100→212、37→98.6 全部通过；顺手发现 VS 的「启动项目」设错了，白跑了四次
- 学会了什么：程序是从上往下一行一行执行的 —— 脑子里想的顺序，代码里必须真的按那个顺序写；变量要先声明才能用；整数除法会把小数部分直接砍掉
- 卡在哪里：① 漏了 `;` 报 C2143 ② `h` 没声明报 C2065 ③ `std::cout << c"\n"` 少一个 `<<` ④ 最费时间的一次：不停改代码，其实跑的一直是 `week01_hello.exe`
- 明天第一步：学 `if` 判断 —— 输入温度低于 0 时额外打印「结冰」
- 用时：约 1.5 小时

---
<!-- 下面是空白模板，每天复制一份放到上面 -->

## 20__-__-__ · Day __
- 今天做了什么：
- 学会了什么（用自己的话）：
- 卡在哪里：
- 明天第一步：
- 用时：__ 小时